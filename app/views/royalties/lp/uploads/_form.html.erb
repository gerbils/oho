<%= bootstrap_form_with(model: [:royalty, upload]) do |form| %>
  <% if upload.errors.any? %>
    <div class="alert alert-danger">
      <h4><%= pluralize(upload.errors.count, "error") %> prohibited this upload from being saved</h4>
    </div>
  <% end %>

  <div>
    <%= form.select :upload_channels, Upload.CHANNELS %>
  </div>

  <div>
    <%= form.file_field :uploaded_file %>
  </div>

  <div>
    <%= form.submit(class: "mt-3 btn btn-primary") %>
  </div>
<% end %>
